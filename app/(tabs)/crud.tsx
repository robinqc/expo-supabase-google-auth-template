import { Button, Text } from "@/components/ui";
import { HeaderView } from "@/components/ui/HeaderView";

export default function CrudScreen() {
    return (
        <HeaderView title="Crud" actionButton={<Button size="sm">Button</Button>}>
            <Text>asd</Text>
        </HeaderView>
    );
}
